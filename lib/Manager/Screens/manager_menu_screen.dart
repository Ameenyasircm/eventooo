import 'package:flutter/material.dart';

class ManagerMenuScreen extends StatelessWidget {
  const ManagerMenuScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
